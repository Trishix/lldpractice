import type { Metadata } from "next";
export const metadata: Metadata = { title: "Practice attempt" };
export default function AttemptsLayout({ children }: { children: React.ReactNode }) { return children; }
